streamlit==1.43.2
numpy==1.26.4
pandas==1.5.2
scikit-learn==1.2.2
scikit-fuzzy==0.4.2
scipy==1.10.1
matplotlib==3.7.1
requests==2.32.3
Flask==3.0.3  # Only if used
Pillow==9.4.0  # Optional, if handling images
pyttsx3==2.90  # Only if using text-to-speech
SpeechRecognition==3.9.0  # Only if needed
pywin32==305; sys_platform == "win32"  # Only needed on Windows
